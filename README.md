# GED Travel Agency

Canada, made accessible.

Educational project — no commercial transactions. Data, prices, events and availability shown in this demo are illustrative.

## Run
Open `index.html` directly in a browser. No build step is required.

Optional local server: `python -m http.server 8000` and open `http://localhost:8000`.

document.addEventListener('DOMContentLoaded', () => { const input = document.querySelector('[data-global-search]'), out = document.querySelector('[data-search-results]'); if (!input) return; input.addEventListener('input', () => { const q = input.value.trim().toLowerCase(); if (!q) { out.innerHTML = ''; return } const cities = (GED_DATA.cities || []).filter(x => x.join(' ').toLowerCase().includes(q)).slice(0, 5); const hotels = GED_DATA.hotels.filter(x => x.join(' ').toLowerCase().includes(q)).slice(0, 5); out.innerHTML = [...cities.map(x => `<a class="result" href="pages/destinations.html?city=${encodeURIComponent(x[0].toLowerCase())}"><span>${x[0]}</span><small>${x[1]} · ${x[2]}</small></a>`), ...hotels.map(x => `<a class="result" href="pages/hotels.html"><span>${x[0]}</span><small>Hotel · ${x[1]}</small></a>`)].join('') || '<p style="color:#777">No matches yet. Try Toronto, Vancouver or Banff.</p>' }); });
